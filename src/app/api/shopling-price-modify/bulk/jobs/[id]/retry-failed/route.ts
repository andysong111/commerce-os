import { NextResponse } from "next/server"; import { requireBulkOperator } from "@/lib/shoplingPriceModifyBulkAuth"; import { bulkRpc } from "@/lib/shoplingPriceModifyBulkDb";
export async function POST(_: Request, context: { params: Promise<{ id: string }> }) { const auth = await requireBulkOperator(); if ("response" in auth) return auth.response; const { id } = await context.params; try { return NextResponse.json(await bulkRpc("shopling_price_bulk_retry_failed", { p_job_id: id, p_owner_id: auth.user.id }), { status: 201 }); } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : "최종 실패 상품이 없습니다." }, { status: 400 }); } }
