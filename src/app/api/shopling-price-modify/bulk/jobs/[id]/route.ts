import { NextResponse } from "next/server"; import { requireBulkOperator } from "@/lib/shoplingPriceModifyBulkAuth"; import { bulkRpc } from "@/lib/shoplingPriceModifyBulkDb";
export async function GET(_: Request, context: { params: Promise<{ id: string }> }) { const auth = await requireBulkOperator(); if ("response" in auth) return auth.response; const { id } = await context.params; try { const data = await bulkRpc("shopling_price_bulk_job_detail", { p_job_id: id, p_owner_id: auth.user.id }); if (!data) return NextResponse.json({ message: "작업이 없거나 접근 권한이 없습니다." }, { status: 403 }); return NextResponse.json(data); } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : "조회 실패" }, { status: 500 }); } }
