import { NextResponse } from "next/server"; import { setBulkJobPaused } from "@/lib/shoplingPriceModifyBulkStore";
export async function POST(_: Request, { params }: { params: Promise<{ jobId: string }> }) { try { return NextResponse.json(await setBulkJobPaused((await params).jobId, false)); } catch (error) { return NextResponse.json({ message: error instanceof Error ? error.message : "재개 실패" }, { status: 400 }); } }
