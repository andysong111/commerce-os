name: Temporary Stage8 Evidence Status Probe

on:
  pull_request:

permissions:
  contents: read

jobs:
  status:
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Read current production mismatch evidence status
        shell: bash
        run: |
          set -euo pipefail
          curl -L -sS --connect-timeout 10 --max-time 60 \
            'https://commerce-os-ops-center.vercel.app/stage8-demand-mismatch-evidence' \
            | python -c 'import html,re,sys; raw=sys.stdin.read(); raw=re.sub(r"<script[\s\S]*?</script>"," ",raw,flags=re.I); raw=re.sub(r"<style[\s\S]*?</style>"," ",raw,flags=re.I); raw=re.sub(r"<[^>]+>"," ",raw); print(re.sub(r"\s+"," ",html.unescape(raw)).strip()[:30000])'
